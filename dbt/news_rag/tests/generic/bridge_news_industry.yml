version: 2

models:
  - name: bridge_news_industry

    columns:

      - name: news_key
        tests:
          - relationships:
              to: ref('fact_news')
              field: news_key

      - name: industry_key
        tests:
          - relationships:
              to: ref('dim_industry')
              field: industry_key